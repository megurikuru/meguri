@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@using System.Globalization

<div style="background-color: #f0f0f0; padding: 15px; margin: 20px 0; border: 1px solid #ccc; border-radius: 5px; color: black;">
	<h4>【デバッグ用】現在の言語設定確認</h4>
	<ul>
		<li><strong>CurrentCulture (地域/書式):</strong> @CultureInfo.CurrentCulture.Name</li>
		<li><strong>CurrentUICulture (画面の翻訳言語):</strong> @CultureInfo.CurrentUICulture.Name</li>
	</ul>
</div>


<div class="home">
	<div>
		@if (SignInManager.IsSignedIn(User)) {
			<a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@L["Hello"] @User.Identity?.Name!</a>
			<form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
				<button type="submit">@L["Logout"]</button>
			</form>
		} else {
			<a asp-area="Identity" asp-page="/Account/Register">@L["Register"]</a>
			<a asp-area="Identity" asp-page="/Account/Login">@L["Login"]</a>
		}
	</div>
</div>